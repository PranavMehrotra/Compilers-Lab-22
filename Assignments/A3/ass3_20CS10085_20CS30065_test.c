#include <stdlib.h>

using namespace std;
typedef struct
{
  if(x>0)while(n);
  int *Arr;
  int queue_size, start_id, end_id;
}QUEUE;//queue structure defined

//This is a test code
        /*
                    to check whitespace
    */

   //To check comments at EOF

for(int i=0;i<n;++i)
{
    printf("hello\n");
    a = '\n';
}